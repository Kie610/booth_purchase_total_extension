# リポジトリ共通ルール

## 適用範囲と優先順位 (Scope and precedence)

- 適用範囲: リポジトリ全体
- 優先順位: ユーザーの明示指示 > 最も近いスコープの指示 > この文書
- 正式な資料: `README.md`、`HANDOFF.agent.md`、`docs/handoff-history.md`
- 改善タスクの指摘一覧と委任プロンプトは `docs/improvement-plan.md` にある。改善作業を依頼されたら最初に読む。
- 可変のブランチ・コミット・進捗・日付付きの検証状態は `HANDOFF.agent.md` だけへ置く。
- 応答、コミットメッセージ、ユーザー向け文面は日本語にする。
- 質問は成果を大きく変える事項だけに絞り、一度に最大3問とする。それ以外は安全側の仮決定を根拠付きで記録して進める。

## プロジェクト契約 (Project contract)

- 利用者: 第三者へ配布する(GitHub Releaseで公開)。
- 互換性を守る公開契約: `ext.storage.local` の保存形式(`boothOrderCache`・`boothOrderIndex`・
  `boothSummary`・`boothTheme`・`boothAvatarAssign` など)、バックアップJSON(`avatarAssign` を含む)、
  CSVエクスポート形式、ユーザーに見える設定。
- 上記以外は内部実装であり、互換層や到達不能コードを残さず変更してよい。
- 公開契約の形式を変えるときは移行手段を先に用意する。内部か公開か判断できないときは削除せず確認する。

## 設計の優先順位 (Design priorities)

規則が衝突したら番号の小さい方を優先する。

1. 動いているものを壊さない。
2. 上記の公開契約の互換性を守る。
3. 現在の要件を満たす最も単純な実装を選ぶ。将来要件の先回りはこれに劣後する。
4. 長期的な構造の一貫性を保つ。

## 設計 (Design)

- まず端から端まで動く最小構成を作り、各段階で動作を保ったまま機能を1つずつ足す。
- 動いている製品を、未完成の作り直しと引き換えにしない。大きな構造変更は着手前に提案する。
- 現在の要件が求めない抽象化・設定・拡張点・間接参照を作らない。
- 各モジュールが変更される理由を1つに保つように分離する。
- 暫定的な実装は内部でのみ許され、永続化データと公開契約では許されない。

## 依存と先行事例 (Dependencies and prior art)

- 共通機能を再実装しない。確立された保守中のライブラリを優先し、使わない場合はその理由を述べる。
- 依存を追加する前に、すでにこのプロジェクトにあるもので解決できないかを確認する。
- 設計の前に、同じ問題を既存の製品やライブラリがどう解いているかを調べ、実績のある方法を採る。
- ライブラリができないと断言する前に、ドキュメントか型定義を読み、読んだものを名指しする。記憶で答えない。

## 変更範囲 (Change scope)

- 依頼された範囲の外を変更しない。ついでの整形・リネーム・リファクタリングは実行せず提案する。
- 自分の変更によって到達不能になったコードの削除は範囲内であり、無関係な変更ではない。
- どの観測可能な挙動が変わるかを述べる。

## 守るべき設計

- 注文IDで重複除去し、注文詳細の「お支払金額」を1注文につき1回だけ集計する。
- 集計済みの結果を唯一のauthoritative sourceとし、UI・CSV出力・共有・バックアップで同じ値を使う。
- 不明・空・失敗・不完全の各状態を区別して表現し、不完全な結果を完全なものとして見せない。
- 置換が成功するまで有効なデータを保持する。新しいデータの確定前に古いデータを消さない。
- `extension/manifest.json`、README、テスト、配布物のversionを一致させる。
- 生成済みアイコンは直接編集せず、変更時は `tools/make_icons.py` を更新して再生成する。

## 安全性と検証結果

- 関係のない変更、他worktree、外部システムを変更しない。
- 未コミット変更、`dist/` の配布物、ローカル設定は所有者を確認するまで上書き・削除しない。
- 秘密情報と不要な個人情報をコミットしない。設定が必要な場合は値を除いた例を使う。
- 構文・ビルド、ブラウザ実行、実環境確認を分けて報告し、未実行項目をPASSと書かない。
- BOOTHへのログイン済み通信や実ページのセレクタは、実測していない限り確認済みとしない。
- reset、上書き、削除、merge、push、公開、deploy、remote作成、権限変更、課金は、対象を読み取りで
  確認し、対象と検証結果を示してユーザーの許可を得てから行う。

## 検証コマンド

- JavaScript構文: 利用可能なNode.jsで `extension/` と `test/` の全 `.js` に `node --check` を実行する。
- 全体テスト: worktree固有ポートで `python -m http.server <port>` を起動し、`/test/index.html` の `ALL PASS` とcheck件数を確認する。
- 配布物: PowerShellで `.\tools\release.ps1` を実行し、ZIP構造、`extension/manifest.json`、SHA-256を検証する。
- 完了を報告する前に、上の該当するコマンドをすべて実行する。

## 保護対象

- `extension/icons/*.png`: `tools/make_icons.py` からの生成物。
- `*.csv`、`booth*.json`: 購入履歴などの個人データとしてコミットしない。
- `docs/handoff-history.md`: 旧HANDOFFの履歴。過去情報を削除せず、新しい状態は `HANDOFF.agent.md` に書く。

## 委任とリリースの境界

- 親セッションが計画・委任・検品・統合・コミットを担い、サブエージェントは委任範囲だけを実装する。
  pushは明示の許可があるときだけ行う。
- 委任、並行実装、リリース、引き継ぎ文書の整備のいずれかを行う前に `docs/agent-appendix.md` を読む。
  検品手順、worktree運用、バージョンブランチとリリース判定、`HANDOFF.md` の保ち方はそこにある。

## 引き継ぎの保守

- 永続ルールはこの文書、場面限定の規則は `docs/agent-appendix.md`、現在状態は `HANDOFF.agent.md` に置く。
- `HANDOFF.agent.md` は引き継ぎ・再開のときだけ読む。通常の作業では読み込まない。
- 実質的な進捗、判断、検証、blockerの変化があったら `HANDOFF.agent.md` を更新する。
- 記録済みのメタデータより、liveなGitと外部の証拠を優先する。欠けている値を捏造しない。
