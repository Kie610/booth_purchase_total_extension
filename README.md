# BOOTHお買いものレポート

BOOTH ([accounts.booth.pm/orders](https://accounts.booth.pm/orders)) の購入履歴から、
実際に支払った合計金額を **注文単位** で正確に集計するブラウザ拡張機能です。

## 直した不具合

既存の公開拡張では、1つの注文で同じショップの商品を複数購入した場合、
購入履歴一覧ページ上でその注文が商品数だけ行として重複表示されることが原因で、
注文の支払金額が複数回加算されてしまう問題がありました。

この拡張では、一覧ページの全ページを巡回したあと **注文IDで重複除去** してから、
注文詳細ページの「お支払金額」(商品代・送料など込みの実際の支払総額)を
1注文につき1回だけ取得して合計するため、重複カウントが発生しません。

## リポジトリ構成

```
extension/          読み込み対象の拡張機能本体
  manifest.json     Manifest V3 定義
  icons/            アイコン(16/32/48/128)。tools/make_icons.py で生成する
  common.js         定数・日付解析・月別集計・支出推移・ストレージ操作の共通処理
  purchase-examples.js
                    X共有用の金額比較マスター(ジャンル・ブランド管理情報を含む)
  csv.js            収集したデータをCSVにする処理(純粋な変換のみ)
  backup.js         バックアップの組み立て・検証・併合(同上)
  popup.html/css/js ポップアップ(収集済み合計の概要表示と集計ページへの導線)
  dashboard.html/css 集計ページ(注文履歴の取得・範囲を指定した金額収集・一括集計・
                    中断・進捗表示・年別月別集計・支出推移・注文内訳・データ出力)
  dashboard-parse.js  BOOTHのページ(HTML)の読み取り
  dashboard-view.js   集計ページの描画(DOMの参照と組み立て)
  dashboard.js        集計ページの状態・イベント・BOOTHからの取得
tools/              開発用スクリプト
  make_icons.py     アイコンの生成(形や色を変えるときはここを直して再生成する)
test/               ブラウザ上で動く簡易テスト(test/README.md 参照)
HANDOFF.md          開発を引き継ぐための資料(調査済みの事項と進め方)
LICENSE             MIT License
```

ビルド手順はありません。`extension/` をそのままブラウザに読み込みます。
アイコンは生成済みのPNGをコミットしてあるため、変更しない限り再生成は不要です。

## インストール方法

### Chrome (Chromium系ブラウザ共通)

1. `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をONにする
3. 「パッケージ化されていない拡張機能を読み込む」から `extension` フォルダを選択

### Firefox

1. `about:debugging#/runtime/this-firefox` を開く
2. 「一時的なアドオンを読み込む」から `extension/manifest.json` を選択

※ Firefoxの「一時的なアドオン」はブラウザ再起動で消えるため、恒久的に使いたい場合は
`about:config` で `xpinstall.signatures.required` を無効化するか、
Firefox向けにMozillaの署名を通す必要があります。

## 使い方

1. `accounts.booth.pm` に事前にログインしておく
2. 拡張機能のアイコンをクリックし、ポップアップの「集計ページを開く」を押す

集計は**専用ページ(タブ)上で実行される**ため、取得中も他のタブやウィンドウを
自由に操作できます。ポップアップを閉じても処理は止まりません。
ポップアップからは進捗と収集済みの合計が確認でき、実行中は
「集計ページを表示」でいつでもページに戻れます。

注文数が多いと一度に全件取得するのは時間がかかるため、処理を2段階に分けて
少しずつ進められるようになっています。

### ① 注文履歴の取得

購入履歴の一覧ページを**新しい順に**巡回し、注文番号・注文日時・ステータスを取得します。
金額はここでは取りに行かないため、比較的短時間で終わります。

**すでに最古まで取得できている場合は、取得済みの注文に到達した時点で停止します。**
2回目以降は増えた分だけを取得するためすぐに終わり、「以降は読み込み済み」と表示されます。

一覧は新しい順に辿るため、途中で失敗すると**新しい方だけが取得され、それより古い注文が
丸ごと欠けた状態**になります。これに気づけるよう、確実に取得できた範囲を内部に記録し、
UIに表示します。

- `取得済みの範囲: 全期間 (最古の注文 2024年2月1日 12:34)` — 最古まで到達済み
- `取得済みの範囲: 最新 〜 2025年8月10日 ...` — 途中で終了。これより古い注文は未取得

**途中で終わっている場合は、既知の注文に当たっても停止せず、最後のページまで辿って
抜けている範囲を取得します。** 中断のあとにもう一度「注文履歴を取得」を押すだけで
穴が埋まるため、全件再取得は不要です(既知の注文はページを読むだけで、取得済みの
金額は取り直しません)。

中断や通信失敗があっても、そこまでに取得できた分は保存した上で「未完了」として記録するので、
範囲の表示が実態と食い違うことはありません。キャッシュが壊れている場合など、
記録そのものを信用できないときは「キャッシュを無視して全件再取得」で最古まで取り直せます。
全件再取得を最後まで完了すると、キャンセル済みや履歴から消えた注文の保存済み金額も整理します。

### ② 金額の収集

注文詳細ページから「お支払金額」を取得します。**年ごとにまとまった表**で
**注文数 / 収集済み / 未収集** の件数が表示されるので、未収集が残っている期間を
見ながら範囲を指定して収集できます。

- 年の行の ▸ をクリックすると、その年の月別を開閉できます(初期状態は閉じています)
- 年の行をクリックすると、その年全体が範囲に設定されます
- 月の行をクリックすると、その月が範囲に設定されます
- 「未収集のある範囲を選択」で、未収集が残る最古〜最新の月をまとめて選択できます
- 収集ボタンの横に、その範囲で実際に取得しにいく件数が表示されます
- 「キャッシュを無視して指定範囲を再取得」にチェックを入れると、
  収集済みの注文も指定範囲だけ取り直します(範囲外のキャッシュは保持されます)
- 注文日時が読み取れなかった注文は月の範囲で指定できないため、
  「まとめて一括集計」で収集します(通常は発生しません)

### まとめて一括集計

①と②をまとめて実行します。従来通りの動作で、「キャッシュを無視して全件再取得」に
チェックを入れるとすべての注文を取り直します。

### 共通

サーバー負荷軽減のため、各リクエストの間に約0.25秒の間隔を空けています。
時間がかかりすぎる場合は「中断」でいつでも止められます。
取得した金額はブラウザのローカルストレージに逐次保存されるため、
中断やタブを閉じた後に再実行すると、未収集の注文だけを取得して続きから進みます。

画面上部にはタイトルが固定表示されます。画面下部には**合計支払額と今年の合計**
(それぞれ収集済みの件数付き、ギフト分は金額の下)が固定表示され、実行中は
その上に進捗バーと「中断」ボタンが出るため、どこまでスクロールしていても
状況が分かります。

画面下部の右端には、黒地に白文字の「𝕏で共有」ボタンがあります。押すとXの投稿画面が新しいタブで
開き、次の文面が入力された状態になります。**投稿は行われません**(Xの画面で
自分で確定します)。金額を公開したくない場合は、投稿前に編集するか閉じてください。

```
BOOTHお買いもの振り返り🛍️

合計：¥196,400（128件）
今年：¥40,130（12件）

積み重なって、Apple iPhone 17 Proが買えるくらいの金額になったようですね

#BOOTHお買いものレポート
```

未収集の注文が残っている場合、この「合計」は実際より少ない額になります。
そのため、未収集があるとき(または注文履歴を最古まで取得できていないとき)は
確認を挟み、**今年分の未収集だけをその場で収集してから、今年の金額だけを共有**します。
合計の行は出しません。買えるものの比較も、文面に出した今年の金額を基準に選びます。

```
BOOTHお買いもの振り返り🛍️

今年：¥40,130（12件）

積み重なって、ReFa BEAUTECH DRYER SMART Wが買えるくらいの金額になったようですね

#BOOTHお買いものレポート
```

集計結果は収集済みの合計額に加えて、**年別・月別の内訳**と注文ごとの一覧を表示します。
年別・月別は合計額の内訳なので同じ枠の中に置き、**畳んだ状態で始まります**
(見出しをクリックで開き、さらに年の行をクリックで月別を展開)。
注文ごとの一覧は件数が多くなるため、折りたたんだ状態で表示され、
展開しても見出し行を固定したスクロール領域に収まります。
一覧では「未収集」(まだ取得していない)と「取得失敗」(取得したが金額を読めなかった)
を区別して表示します。取得失敗の注文は次に集計を実行したときに自動で取り直すので、
全件再取得をしなくても直ります。

### キャッシュ削除

ページ最下部の「キャッシュ削除」を開くと、**注文履歴**と**収集した金額**を
それぞれ個別に削除できます。現在の保存件数が表示され、削除前に確認を求めます。
消したデータはBOOTHから取り直すことになるため、再取得には時間がかかります。

## 集計対象について

- デフォルトで **キャンセルされた注文のみ除外** し、それ以外(発送完了・支払済み・未払いなど)は
  すべて合計に含めます。
- 集計は注文詳細ページの「お支払金額」を用いるため、送料やBOOST(投げ銭)分も
  含めた実際の支払総額になります。
- 支払額に**ギフト分が含まれる場合は、その金額を支払額の左に小さく併記**します
  (合計・年別・月別・注文ごとの内訳・画面下部の固定表示のすべて)。ギフト分は
  支払額に含まれた内訳なので、合計から差し引く扱いにはしていません。
  ギフトが無い注文では何も表示しません。
- ギフト分は、注文詳細ページで「ギフト」の見出しが付いた商品グループに並ぶ
  商品価格(BOOST含む)の合計です。ページ構造が変わって商品を判別できない
  場合は、0円と断定せず「不明」として扱い、何も表示しません。

## 画面の切り替え

ヘッダー左の「☰」から画面を切り替えられます。**収集の実行中でも切り替えられ、
処理は止まりません**(同じページの中で表示を出し分けているため)。

- **お買いものレポート** — 収集と集計を行う既定の画面
- **推し作者ランキング** — ショップごとの購入商品数と金額を多い順に並べる画面(𝕏で共有できます)
- **支出推移・前年比較** — 選んだ2つの年の月別支出、同期間比、年間累計を確認する画面
- **今年のまとめ** — 1年を振り返る画面(𝕏で共有できます)
- **データ出力** — 収集したデータをCSVで保存する画面
- **データの引っ越し** — バックアップの保存と復元を行う画面

レポート以外の画面では、未収集の注文が残っている場合や注文履歴の取得が途中で
終わっている場合に、**その画面の数字が実際より少なくなることを表示**し、
収集できる画面へ戻るリンクを添えます。

## 今年のまとめ

「今年のまとめ」画面では、選んだ1年を振り返ります。見出しの横のプルダウンで年を
選べます(注文のある年だけが並びます)。今年まだ注文が無い場合は、最も新しい年を
最初に表示します。

| 項目 | 内容 |
|---|---|
| 〇〇年の合計額 | その年のお支払金額の合計。注文件数、ギフト額、未収集の件数を添える |
| 買ったもの | 商品の点数(数量ぶん数えます)。ギフトの点数を添える |
| 支援した作者 | 買ったショップの数。**その年にはじめて買ったショップの数**を添える |
| いちばん買った月 | その年で支払額が最も多かった月 |

支払額を取得できていても商品明細を読めていない注文がある場合は、合計額とは別に
「商品明細未収集」と表示し、点数・作者数・推し作者が少なくなる可能性を共有前にも確認します。

続けて、その年の**推し作者を上位3ショップ**まで表示します。

**「はじめて買った作者」の数には注意点があります。** その年より前の注文が未収集だと
商品の明細が無いため、実際には前から買っていた作者を「はじめて」と数えてしまいます。
その状態のときは画面に件数とともに断りを表示します。

作者ごとの金額は「推し作者ランキング」と同じく商品の合計(単価×数量+BOOST)なので、
送料やクーポンが入りません。足しても上のカードの合計金額とは一致しません。

この画面を開いている間は、画面下部の共有ボタンが**「𝕏で〇〇年のまとめを共有」**に
変わります。

## 共有カード(画像)

「𝕏で共有」を押すと、投稿する文面と一緒に**共有カードの画像**が表示されます。
合計・推し作者ランキング・今年のまとめのどれを共有するかによって中身が変わり、
**画面に出ているものと同じ数字**で作られます。

**𝕏の投稿画面には、画像を自動で添付できません。**投稿画面に渡せるのは文面だけです。
そのため次の手順になります。

1. 「画像をコピー」または「画像を保存」を押す
2. 「𝕏の投稿画面を開く」を押す
3. 投稿画面で画像を貼り付ける(保存した場合はファイルを添付する)

### 画像の形を選ぶ

**16:9 / 1:1 / 4:3 / 3:4** から選べます。既定は𝕏のタイムラインで切り取られない16:9です。
正方形や縦長では数字が横に潰れるため、自動で2列に折り返します。

### 背景テンプレートを選ぶ

**色と模様を別々に選び、その組み合わせが背景になります。**

- 色は7種類(赤・オレンジ・黄・緑・青・紫・ピンク)
- 模様は7種類(グラデーション・水玉・斜めストライプ・格子・市松・波・フレーク)

模様は赤で一度だけ組み、他の色は**色相だけを回して**作っています。そのため、
どの色を選んでも模様の太さや間隔は変わりません。

見本のボタンには、**いま選んでいるもう一方を当てはめた状態**が出ます(色の見本には
選んでいる模様が、模様の見本には選んでいる色が乗ります)。組み合わせた結果を
選ぶ前に確かめられます。

模様は**カードの中心を基準に**敷くので、どの縦横比でも中心について点対称になります
(フレークだけは散らばりが持ち味なので対称にしていません)。すべてその場で
描いているので、外部の画像は読み込みません。

### 背景画像を差し替える

「画像を選ぶ」から手持ちの画像を指定するか、**すぐ下の枠に画像をドロップ**すると、
カードの背景になります。画像を選んでいる間はテンプレートより画像が優先されます。画像は縦横比を保ったまま全面に収まるよう配置し、
文字が読めるように上から暗く重ねます。「元に戻す」でテンプレートの背景に戻ります。

**選んだ画像はそのタブの中だけで使い、保存も送信もしません。**画面を閉じると
消えるので、共有のたびに選び直してください。

## 支出推移・前年比較

「支出推移・前年比較」画面では、収集済みの注文を使って次の内容を表示します。

- 選んだ年の1月から対象月までの支払額と、比べる年の同じ期間の支払額
- 比べる年から増減した金額と割合
- 2つの年の月ごとの支出を並べた棒グラフ
- 月ごとに積み上がる年間累計グラフ
- **買った曜日と時間帯**のヒートマップ
- お買いものレポートと同じ年別・月別の集計表

見出しの横で**比較する2つの年**を選べます(注文のある年だけが並びます)。
既定は今年と前年です。同じ年どうしは比べられないので、選んだ年は相手側の候補から外れます。
今年を選んだときは今月まで、過ぎた年を選んだときは12月までを対象にします。

グラフと表はどちらも、注文単位の「お支払金額」を同じ集計条件でまとめています。
外部サービスへの送信や外部ライブラリの読み込みは行わず、ブラウザ内に保存されたデータだけで描画します。
未収集の注文や取得途中の注文履歴がある場合は、実際より少ない金額になる旨と収集画面への導線を表示します。

### 買った曜日と時間帯

曜日(縦)×時間帯(横)のマスに、**注文の件数**を色の濃さで表します。金額ではありません。
高額な1件でマスが真っ赤になると「その時間によく買う」ようには読めないためです。

対象期間は、**上の「比較する年」で左に選んだ年に合わせて切り替わります**。上のグラフと
違う期間のものが並ぶと、同じ画面の中で食い違ったものを見比べることになるためです。

期間はその年の**1年間**です(今年を選んだ場合も今月では切りません)。ここで見るのは
曜日と時間帯の傾向なので、金額を足し合わせる上の比較と違い、月をそろえる必要が
ないためです。その年に注文の無い月は選べないので、**実在する範囲へ寄せて**選びます。

別の期間で見たいときは、月単位の範囲を選び直すか「全期間」ボタンを押してください。
選び直した範囲はそのまま残り、**年を切り替えたときだけ**その年に合わせ直します。

注文日時から曜日または時刻を読み取れない注文はマスに入れず、件数を表示して断ります。
0件として置くと、日曜0時にまとめ買いしたように見えてしまうためです。

## 推し作者ランキング

ショップ(作者)ごとに、買った商品の点数と金額を多い順に並べます。**上位10ショップ**まで
表示し、1〜3位は順位の背景に金・銀・銅の王冠を敷き、5位までを太字にします。
ギフトで買った分は、商品数・金額のどちらも左に小さく併記します。

見出しの横の**「金額編」「購入数編」**で並べ替えの基準を切り替えられます。少額の商品を
たくさん買ったショップと、高額の商品を1点だけ買ったショップでは順位が入れ替わるため、
どちらで並べているかは押されているボタンと、色の付いた列の見出しで分かります。

**順位をクリックすると、そのショップで買った商品が下に開きます。**同じ商品を複数の注文で
買っていても、名前でまとめて1つだけ並べます。開いた状態は並べ替えても保たれます。
「今年のまとめ」の推し作者も同じように開けます。

### ランキングを𝕏で共有する

この画面を開いている間は、画面下部の共有ボタンが**「𝕏でランキングを共有」**に変わり、
上位5ショップを投稿画面に渡します。切り替えている基準(金額編・購入数編)の並びを
そのまま共有するので、画面に出ている順位と食い違いません。

**金額や購入数を出したくない場合**は、表の上のチェックボックスを入れると順位と
ショップ名だけの文面・共有カードになります。金額編で金額を出す場合は、上記のとおり送料や
クーポンを含まない金額なので、その断りを文面にも添えます。

未収集の注文が残っている、注文履歴の取得が途中で終わっている、金額を読み取れなかった
商品があるといった場合は、**順位そのものが実際と違う可能性がある**ため、共有する前に
確認を挟みます。

**この画面の金額だけは、他の画面と計算のもとが違います。**「お支払金額」は注文に1つしか
なく、1つの注文が複数のショップにまたがるとショップごとに割り振れないため、
**商品の合計(単価×数量+BOOST)**で集計しています。送料やクーポンは含まれないので、
ショップ別の金額をすべて足しても、画面下部に出る合計支払額とは一致しません。

## データ出力(CSV)

「データ出力」の画面から2種類のCSVを保存できます。Excelでそのまま開けるよう
UTF-8のBOM付きで書き出し、金額の列には桁区切りを付けません。

| 種類 | 1行の単位 | 列 |
| --- | --- | --- |
| 注文単位 | 1注文 | 注文番号 / 注文日時 / ステータス / お支払金額 / ギフト額 / 商品合計 / 送料 / 差額 / 商品点数 |
| 商品単位 | 1商品 | 注文番号 / 注文日時 / ステータス / ショップ名 / ショップURL / 商品名 / 単価 / 数量 / BOOST / ギフト |

- 未収集や取得失敗の注文は、0円と区別が付かなくなるため**空欄**にします。
  商品単位のCSVでも、その注文を買っていないように見えないよう行自体は残します。
- 合計は**注文詳細ページの「お支払金額」**から計算しており、商品単価の合計では
  代用していません。配送のある注文には送料が乗るため一致しないからです
  (実例: お支払金額¥4,060 = 商品¥890×4個 + 送料¥500)。
  お支払金額を商品合計と送料で説明しきれない分は「差額」列に出て、画面にも
  件数を表示します(クーポンやポイントなどが考えられます)。

## データの引っ越し(バックアップ／復元)

注文が多いとBOOTHからの再取得に数分かかります（注文1000件で4分以上）。
ブラウザやプロファイルを移すときは「データの引っ越し」の画面から、収集済みの
データをJSONファイルに保存し、移行先で読み込み直してください。
保存されるのは注文履歴と収集した金額だけで、**BOOTHのログイン情報は含まれません**。

復元は入れ替えではなく**併合**です。今あるデータは消えず、同じ注文があれば
収集済みの方を残します。注文履歴の「取得済みの範囲」は、**両方が全期間のときだけ**
全期間のままにします（片方でも途中までだと、つないだ結果に抜けが無いとは
言い切れないため）。読み込んだファイルは、対応バージョンと注文履歴・金額・商品明細の
形式を確かめてから使い、バックアップ以外のファイルや壊れたファイルでは何も書き換えません。

## 集計対象について(補足)

- 商品明細(ショップ名・商品名・単価・数量・BOOST・ギフトかどうか)と送料は、
  金額の収集と同時に注文詳細ページから取得します。
- ダウンロード商品と配送商品では注文詳細ページの構造が異なりますが、どちらにも
  対応しています。

## 既知の制限

- 拡張機能の更新で、注文詳細ページから保存する項目が増えることがあります。その場合、
  以前の版で収集した注文は「未収集」として扱われ、**再実行すれば自動的に取り直します**
  (「キャッシュを無視して」の指定は不要です)。取り直しの対象があるときは、
  「② 金額の収集」に件数と理由を表示します。
  - 金額の集計(合計・年別・月別)は取り直しの前後で変わりません。増えるのは
    商品明細まわりの情報だけです。
  - 保存された内容から「この注文は取り直しが必要か」を見分けることはできないため、
    **以前の版で収集した注文はすべて対象になります**。注文1000件なら4分以上かかります。
  - これまでに項目が増えたのは次のタイミングです。
    - v0.14.0 商品明細(ショップ名・商品名・単価・BOOST・ギフトかどうか)を追加
    - v0.16.0 数量と送料を追加(v0.15.1で配送商品に対応したときに増えた項目)
- 集計ページのタブを閉じると処理は中断されます(Service Workerでの完全なバックグラウンド
  実行には未対応)。ただし取得済みの金額は保存されているため、再実行すれば続きから取得します。
- 一時的な通信エラーは数回試し直します。それでも取得できなかった注文は**飛ばして
  先へ進み**、件数をまとめて知らせます。飛ばした注文は「未収集」のまま残るので、
  再実行すれば自動で拾い直します(数百件の収集が1件の失敗で止まらないようにするため)。
  ログイン切れの場合だけは、続けても全部失敗するのでその場で止めます。
- BOOTHのページ構造(HTML)が変更された場合、セレクタの調整が必要になる可能性があります。
  一覧をうまく読み取れなかった場合(1件も取れない、ページ送りを追えない)は、
  「全期間を取得済み」とは表示せず警告を出します。合計が実際より少なくなっている
  可能性があるためです。
- 集計中、取得1件につき以下のCSP違反がブラウザのコンソールに記録されます。

  ```
  Loading the script 'https://www.google.com/recaptcha/enterprise.js?...'
  violates the following Content Security Policy directive: "script-src 'self'"
  ```

  BOOTHのレスポンスに `Link: <...enterprise.js>; rel=preload; as=script` ヘッダが
  含まれており、ブラウザがヘッダの時点で先読みを試みるためです。拡張機能ページの
  CSPが要求の発行前にブロックしているため**通信は発生せず、集計結果にも影響しません**。
  fetch側から先読みを抑止する手段は無く、CSPに `google.com` を追加すれば消せますが、
  それは読み込みを許可する意味になりMV3のリモートコード禁止に抵触するため行いません。

## プライバシー

購入履歴・金額はすべて閲覧中のブラウザ内(`storage.local`)にのみ保存され、
外部へ送信することはありません。取得した購入データはこのリポジトリには含めません。

## ライセンス

[MIT License](LICENSE)
