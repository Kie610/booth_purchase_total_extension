# 改善計画と委任プロンプト集(2026-08-07 レビュー)

v1.0.0 リポジトリ全体の敵対的検証の結果と、改善タスクを他エージェントへ委任するための
自己完結プロンプトをまとめる。永続ルールは `AGENTS.md`、現在状態は `HANDOFF.agent.md` を正とする。

## 検証サマリ

- 実施日: 2026-08-07 / 対象: ブランチ `1.0.0`(clean、v1.0.0リリース済み)
- status=PASS; kind=compile; command=`node --check` を extension/ と test/ の全16 `.js` へ実行; counts=passed=16, failed=0
- status=PASS; kind=runtime; command=`python -m http.server 8742` + Browserで `/test/index.html`; scope=全体テスト; counts=`ALL PASS (640 checks)`, failed=0
- 画面確認: extension一式をscratchpadへ複製し、テスト用スタブ+デモデータ(注文90件・2024〜2026年)を注入して
  `dashboard.html` を実ブラウザ(ユーザーのChrome、1568px幅)で表示し、レポート・ランキング・支出推移・
  まとめ・ドロワー・共有パネルをスクリーンショットで目視確認した(リポジトリは未変更)。
  データ出力・引っ越し画面と620px幅以下の表示は構造確認のみで、目視は未実施。
- 未実施: BOOTHログイン済み実ページでの通信・セレクタ確認(従来どおり U1)。`tools/release.ps1` は今回未実行。

総評: 集計の正しさ(注文ID重複除去・不明値をnullで扱う・未収集の明示)への配慮は一貫して高品質。
テスト・アクセシビリティ(フォーカストラップ、aria、inert)も水準以上。以下は減点部分の列挙。

## 指摘一覧

### A. 不具合・不整合(いずれも軽微、データ破壊なし)

- A1 【共有】確認ダイアログの文言と実挙動のずれ。`shareConfirmMessage()` は「今年の金額だけを共有します」と
  案内するが、未収集が今年分のみの場合、収集完了後に `canShareTotal()` が真になり `buildShareText()` は
  全期間の合計も含めて共有する(`extension/dashboard.js` の shareBtn ハンドラ + `extension/share.js`)。
  挙動自体はより正確な数字を出すので、文言側を「取得後に全期間の合計を共有できる場合はそちらを共有します」等へ直すのが妥当。済(1.1.0)
- A2 【進捗】`collectAmounts()` の進捗バーが `index / targets.length` で、テキスト `(index+1/N)` と1件ずれ、
  最終件でも100%にならない(`extension/dashboard.js`)。済(1.1.0)
- A3 【タブ管理】集計ページを2枚開くと `boothDashboardTab` が後勝ちし、片方を閉じた `pagehide` でキーが
  無条件に消えるため、残ったタブがあってもポップアップから新規タブが開く(`extension/dashboard.js` init/pagehide、
  `extension/common.js` openDashboard)。自タブ IDと一致するときだけ消す形にすれば解消。済(1.1.0)
- A4 【HTML】`dashboard.html` / `popup.html` に `<meta name="viewport">` が無い。拡張タブでは実害が薄いが、
  モバイル系ブラウザ(Firefox for Android等の将来対応)で980px仮想幅で描画される。済(1.1.0)
- A5 【レイアウト】曜日×時間帯ヒートマップ(25列grid)に横スクロール用ラッパーが無く、狭い窓では
  セルが `minmax(0,1fr)` で潰れる。注文内訳テーブルの `.table-scroll` と同じ扱いにする。済(1.1.0)

### B. 無駄な処理・効率(現状の数百件規模では体感差なし。数千件で効く)

- B1 `render()` 1回の中で `buildResults()` が各描画関数から計8回前後呼ばれ、毎回全注文を走査して
  新しい配列を組み立てる。render() 冒頭で1回組んで各関数へ渡す(または描画サイクル内メモ化)。済(1.1.0)
- B2 `renderOrderTable()` が検索欄の1キー入力ごとに全行のDOMを再構築し、`shop-asc` ソートは比較のたびに
  `orderShopsLabel()` を再計算する。入力のdebounce(150ms程度)と、ソートキーの事前計算で軽くなる。済(1.1.0)
- B3 `collectAmounts()` が5件ごとに `saveCache()` でキャッシュ全体を書き直すため、注文n件の収集で
  O(n²/5) のシリアライズになる。フラッシュ間隔を件数比例にするか、フラッシュ件数を増やすだけでも改善する。済(1.1.0)
- B4 `oldestCoveredOrder()` がループ内で暫定最古の `parseOrderDate()` を毎回再計算する(微小)。済(1.1.0)

### C. 画面設計の改善点(優先度順)

- C1 【ナビゲーション】6画面すべてがハンバーガードロワーの中にあり、900px幅のデスクトップでも
  ランキング・まとめ・支出推移の存在に気付きにくい。広い画面(≥760px目安)ではヘッダー直下に
  水平タブとして常時表示し、狭い画面のみドロワーへ畳むのが本命の改善。済(1.1.0)
- C2 【初回体験】データが無い初回はリード文と「一括で集計を実行」だけが出る。空状態に
  「① BOOTHへログイン → ② 一括集計 → ③ レポート・ランキングを見る」の3ステップ案内を置くと迷わない。済(1.1.0)
- C3 【月別テーブル】年行クリック=範囲設定、▸クリック=開閉、月行クリック=範囲設定という
  2役の操作がhint文頼み。範囲設定された行のハイライト(既存の .in-range)に加え、
  行末に「選択」の明示的な手掛かり(小ボタンまたはラジオ風アイコン)を足すと学習コストが下がる。済(1.1.0)
- C4 【グラフ】「月ごとの支出」棒グラフに数値軸・値ラベルが無く、title属性のツールチップのみ。
  ホバーできない環境では読めない。累計グラフと同様のY軸目盛(0/50%/100%)を足す。済(1.1.0)
- C5 【KPI表示】メインの「収集済みの合計額」が28pxでフッターと差が小さい。36〜40pxへ拡大し、
  ページの主役であることを示す。済(1.1.0)
- C6 【共有パネル】色7×模様7のテンプレート+画像アップロード+拡大率で縦に長い。
  「背景をカスタマイズ」としてテンプレート以下を折りたたみ、既定はプレビュー+文面+3ボタンに絞る。済(1.1.0)
- C7 【バナー】pendingBanner がレポート以外の全画面に常時出続けるため慣れて無視されやすい。
  件数が変わらない間は閉じられるようにする(閉じた状態はセッション内のみ保持で十分)。済(1.1.0)
- C8 A4/A5(viewport・ヒートマップ)もUI項目として同時に扱う。済(1.1.0)
- C9 【ダイアログ】共有前の確認・キャッシュ削除の確認がネイティブ `confirm()` で、作者情報・共有パネルの
  丁寧なアプリ内モーダル(フォーカストラップ・inert)とデザインが不一致。アプリ内確認ダイアログへ置き換える。済(1.1.0)
- C10【KPI表記】メイン合計のギフト表記が「ラベルとギフトと金額」が横一列で、ギフト額が本体金額の一部に
  見えやすい。ギフト表記は金額の下の補足行へ移す。済(1.1.0)
- C11【ランキング】順位バッジ横の ▸(商品明細の開閉)が小さく淡いため、開けることに気付きにくい。
  ▸をショップ名側にも効かせるか、「商品を見る」のテキストリンクを添える。済(1.1.0)

### C'. 共有パネルの再設計(2026-08-07 ユーザーフィードバック。C6の方向性を上書き)

背景: 利用者層(VRChatter)は写真をよく撮り、写真を自分の成果物として添えて共有することが多い。
その方が見栄えも良い。よってC6の「カスタマイズ一式を畳む」は誤りで、**画像設定こそ最短動線**にする。

- C12【画像ファースト】背景画像の設定(画像を選ぶ+ドロップ)をアコーディオンから出し、
  プレビュー直下の一等地に置く。色・模様テンプレート(写真を使わない人向けの下地)は
  折りたたみのままでよい。拡大率スライダーは画像設定時に見える位置へ。済(1.1.0)
- C13【操作ボタンの固定】共有パネルが縦に長くスクロールが必要なため、「画像をコピー」「画像を保存」
  「𝕏の投稿画面を開く」をパネル下部に固定配置(sticky)し、本文だけをスクロールさせる。済(1.1.0)
- C14【プレビューへのドロップ】ドロップゾーンだけでなく、プレビューcanvas自体への
  ドラッグ&ドロップでも背景画像を設定/差し替えできるようにする(dragover中は視覚的な合図を出す)。済(1.1.0)
- C15【文面の優先度を下げる】投稿文面はXの投稿画面で編集できるため、UI上の優先度を下げる
  (パネル下方へ移動、または折りたたみ)。削除はしない。済(1.1.0)

### C''. 共有パネルの操作性調整ほか(2026-08-07 ユーザーフィードバック第2弾)

- C16【形と拡大率を上へ】画像の形(縦横比)ボタンと拡大率スライダーを**横並び**にし、
  背景画像セクションの**上**へ移す(ドロップゾーンはある程度見えていれば十分)。済(1.1.0)
- C17【タブ切り替え】「カスタム(画像)設定」と「テンプレート(色・模様)」をタブ風UIで
  切り替える(アコーディオンをやめる)。済(1.1.0)
- C18【文面の常時表示】操作ボタンが下部固定になったため、投稿文面のアコーディオンをやめて
  常時表示へ戻す(C15の折りたたみを解除。位置は下方のままでよい)。済(1.1.0)
- D10【物理アイテムのステータス再取得】デジタルでない注文はステータスが後から変わる
  (支払済み→発送完了 等)。①注文履歴の取得に「物理アイテムのステータスを再取得」
  チェックボックスを追加し、ONのとき既知注文のステータスを一覧ページから更新する。
  全件再取得(キャッシュ破棄)とは別物で、金額キャッシュは保持する。済(1.1.0)
- T1【harness同期テスト】test/index.html の雛形要素と extension/dashboard.html の
  同一要素のclass一致を突き合わせるテストを追加し、「CSSが効かず実表示テストが素通りする」
  穴の再発を防ぐ。済(1.1.0)

### D. 追加機能・改良アイデア(小粒含む)

- D1 ダークモード対応。配色はCSS変数へ集約済みなので `prefers-color-scheme: dark` の変数上書きが主作業。
  共有カードは現状のライト配色を維持してよい(画像出力はテーマに依存させない)。済(1.1.0)
- D2 注文内訳の注文番号を `https://accounts.booth.pm/orders/{id}` へのリンクにする(rel=noopener)。済(1.1.0)
- D3 収集中にタブタイトルへ進捗を出す(`document.title = "(35/120) 収集中…"`)。権限追加なしで
  他タブ作業中でも進捗が分かる。終了時に元へ戻す。済(1.1.0)
- D4 ポップアップに「今年の合計」も表示する(summaryへ yearTotal を足すだけ)。済(1.1.0)
- D5 ランキングに期間(年)フィルタを追加し「今年の推し作者」をランキング画面でも見られるようにする。済(1.1.0)
- D6 注文内訳の絞り込み結果だけをCSV出力するボタン(データ出力と導線を共有)。
- D7 月/年の予算設定と、フッターでの残額・超過表示(ストレージに設定1キー追加)。
- D8 ヒートマップのセルクリックで該当曜日・時間帯の注文を内訳テーブルの絞り込みで表示。
- D9 Firefox恒久対応(browser_specific_settings.gecko 追加と署名手順整備)。既知の未対応項目U2の解消。
  不採用(2026-08-09決定): 調査の結果、コード変更は小さい(gecko.id追加・host_permissions権限案内・
  strict_min_version 127)が、全Firefox配布にMozilla署名が必須でAMOアカウントとリリースごとの
  署名運用が恒常コストになる。正式対応はChromeのみとし、Firefoxは about:debugging の
  一時読み込み運用に限定する(READMEへ記載済み)。再提案しない。
- D11【テーマ切り替え】(2026-08-07 ユーザー要望)ヘッダーにライト/ダーク/システム(OS設定に従う)の
  3状態のテーマ切り替えを設置する。選択はストレージへ保存し、集計ページとポップアップの両方が従う。
  既定はシステム。共有カード(canvas)はライト配色固定のまま。 済(1.1.0)

### D'. VRChatter調査にもとづく追加機能(2026-08-08 ユーザー承認済み)

背景: 利用者層(VRChatter)の調査より。詳細な調査経緯と実測は HANDOFF.agent.md / 会話記録を参照。
実装順の推奨は D12+D13(確度高・小規模) → D14(大型)。

- D12【ギフトフィルタ】共通フィルタ「集計対象: すべて / 自分用 / ギフト」をsegmented(D11と同部品)で
  設置し、buildResults() の入口で items[].gift(取得済み)により絞る。全ビュー・CSV・共有文面/カードが
  自動追従する。ギフト/自分用モードの金額は内訳ベース(price×quantity+boost)になり注文単位の
  お支払金額と送料・クーポン分ずれるため、差額行を明示する(隠さない)。選択は保存せず既定「すべて」。
  ポップアップは現行のギフト補足行のまま。共有時は「🎁贈ったギフト」等の見出しを付ける。済(1.1.0)
- D13【伏せ字共有】共有パネルに「品名・ショップ名を出さない」チェックを追加。ONのときカードは
  合計額・件数・期間・月別棒グラフのみ(ショップランキング枠は非表示)、文面もショップ名抜きの定型へ。
  D12のフィルタと直交して併用可。済(1.1.0。ランキングカードの月別棒グラフのみ月次データ経路がなく見送り)
- D14【沼レポート(アバター別支出内訳)】キャッシュ済み items[].name にはバリエーション名が
  「商品名 (バリエーション名)」形式で既に含まれる(2026-08-08 ユーザー実環境で実測確認。
  例:「(エク(ミルフィ))」「(Milfy&Eku)」「(【Full Pack】)」。単一バリエーション商品は
  商品名と同文が付く)。末尾括弧を剥がしてバリエーション部を取り出し、素体名辞書
  (日本語+ローマ字)と照合してアバター別の累計・件数ランキングを出す。
  Full Pack・複数名・不一致は「複数対応」「未分類」として別枠に明示し、商品単位の
  手動割り当てUI(選択はストレージ保存=公開契約への追加)を受け皿にする。
  商品ページへのアクセス・URL対応マスターは作らない(MUST NOT)。
  第2シグナル(注文詳細のダウンロードファイル名行。実在確認済み)はパーサ拡張+キャッシュ項目
  追加が必要なため任意の後続とする。共有カードへの「最推しアバター」表示も本項に含む。
  済(1.1.0)。実装メモ: 辞書は `extension/avatar-master.js`(33体・データのみ)。パーサと集計は
  `common.js`(parseItemName・matchAvatarKeys・classifyItemAvatar・aggregateByAvatar)。
  新ビュー `#/avatars`(沼レポート)を追加し、期間・基準はランキングと同じ作法。
  手動割り当ては新キー `boothAvatarAssign`(公開契約に追加)とバックアップの
  `avatarAssign`(BACKUP_VERSIONは1のまま。省略可能項目なので旧版も読める)。
  伏せ字ONでもアバター名は出す(根拠は share.js の沼レポートの節)。
  第2シグナル(ダウンロードファイル名行)は未着手のまま。
  **D17で照合方式を置き換え**(固定辞書 → 辞書レスのトークンクラスタリング)。集計・表示・
  保存キーの枠組みはD14のまま使う。

### D''. 敵対的検証にもとづく追加改善(2026-08-09)

背景: 2026-08-09の敵対的検証。既存機能が「静かに劣化する」「部分集計を全体に見せる」経路を塞ぐ。

- D15【収集健全性の警報】BOOTHがHTML構造を変えると collectAmounts が静かに劣化し、注文ごとの
  「取得失敗」は出ても全体として構造変更を疑う手掛かりが無い。1回の収集の終了時(中断含む)に、
  実際に取得を試み切った注文のうち金額(amount)または商品明細(items)を最終的に読み取れなかった
  件数と割合を数え、「3件以上 かつ 試行の30%以上」で警告バナーを出す。文言に件数を入れ、
  BOOTH側の構造変更の可能性と更新情報の確認を促す。バナーは閉じられる(pendingBannerと同じ作法)。
  閾値・状態はストレージへ保存せず次の収集で計算し直す。個別の「取得失敗」表示と needsCollect による
  自動再取得の挙動は変えない。済(1.1.0)。実装メモ: 判定は `common.js` の `collectHealthAlert()`
  (純関数)。計数は `dashboard.js` の `collectAmounts()` 内で try/finally により中断時も記録し、
  `setCollectHealth()` で `dashboard-view.js` の状態へ渡す。再試行して成功した通信エラーは
  数に入らない(fetchDocWithRetry の成功経路を通るため)。収集はレポート画面で行うので、
  pendingBanner と違い全画面(レポート含む)で出す。
- D16【CSVへの集計対象注記】D12のギフトフィルタ適用中に書き出したCSVは部分集計だが、ファイル単体を
  見るとそれが分からない。フィルタが「すべて」のときはCSV形式(公開契約)を1バイトも変えず、
  「自分用」「ギフト」のときだけ注文CSV・商品明細CSVの末尾へ列「集計対象」を追加し、ヘッダーと
  全データ行に値を入れる。ファイル名にも `booth-orders-gift-YYYYMMDD.csv` の形で集計対象を入れ、
  データ出力の画面にも部分集計である旨の注記を出す。済(1.1.0)。実装メモ: 列とファイル名は
  `csv.js`(`giftFilterCsvLabel` / `buildOrdersCsv` / `buildItemsCsv` / `csvFileName` の
  任意引数 `filter`)。列名は `common.js` の `GIFT_FILTER_CSV_COLUMN`。画面の注記は
  `dashboard-view.js` の `renderExportArea()`。

### D'''. 沼レポートの照合方式の置き換え(2026-08-09 ユーザー実データで実測)

背景: 2026-08-09にユーザーの実CSV637明細とBOOTH実ページの74体名簿で実測した結果、D14の
固定辞書33体では単一アバターを特定できたのが金額の15.5%にとどまり、辞書に無い「凪」は
1点も拾えなかった。アバターは数万体あり毎日増えるため、辞書の網羅は原理的に不可能。
同じデータでトークン完全一致クラスタリング+表記ゆれ統合を試すと28.3%へ改善し、「凪」が
最大バケツとして自動で出現した。実在バリエーション73本(3ショップ3書式)で100%正解。
部分一致(includes)は「凪」が実在アバター「凪夜(Nagiya Ruri)」へ構造的に誤ヒットする
(74体中この1組)。トークン完全一致ではこの衝突が起きない。

- D17-a【辞書レスのアバター照合】D14の「固定辞書へ当て込む」照合を、買った商品どうしを
  突き合わせてアバターのまとまりを見つける方式へ置き換える。バリエーション名(無ければ品名
  本体)をNFKC正規化・小文字化し、英字列/かな列/漢字列をトークンに分割する(カタカナは
  ひらがなへ寄せ、末尾の「対応」「用」「向」「ちゃん」を剥がし、1文字の英字・かなは捨て、
  漢字1文字は残し、ストップ語を除く)。照合は**トークン完全一致のみ**で部分一致は使わない。
  バケツの採用は「名簿に載っている」または「2つ以上の別商品(ショップ+品名本体で判定)の
  **バリエーション名**に現れた」のどちらか。名簿(約86体)は網羅用の辞書ではなく、
  表記ゆれの統合と表示名のヒント。
  同一バリエーション文字列内の「和文1語＋英字1語」の併記を共起として数え、2商品以上で
  共起した組を統合候補として手動割り当てUIへ出す(「Milfy, Eku」のような別アバターの併記と
  区別できないため自動確定はしない)。判定順・複数対応・未分類・保存形式はD14のまま。
  済(1.1.0)。実装メモ: `common.js` の `avatarTokens`・`avatarKeysIn`・`buildAvatarIndex`・
  `avatarDisplayName`(旧 `matchAvatarKeys`・`avatarAliasMatches`・`avatarByKey` は削除)。
  名簿は `extension/avatar-master.js` の `{jp, en, alt}` 形式へ変更し、`en` が保存キー。
  P10で保存された旧キー(milfy・maya・rindo・kokoa・chise など)は名簿の `en`/`alt` に
  残してあるため名前付きのまま読める。名簿に無いキーの手動割り当ても未分類へ落とさず、
  キーをそのまま表示名にして通す(本人の指定を消さない)。
  ノイズ対策(2026-08-09の検品で、実データ637明細から名簿外バケツが131個できて大半が
  一般語だったため追加): (1) 新規バケツの採掘元をバリエーション名だけに絞る
  (hair・bob・pose・devil 等のノイズは品名本体由来だった)。品名本体は既知バケツの
  照合には引き続き使う。(2) 純数字・「v/ver+数字」・英字2文字以下・名簿に無い漢字1文字を
  トークン段階で捨てる。(3) ストップ語を実測ノイズで拡充する。ストップ語は
  トークンと同じ正規化(NFKC・小文字化・カタカナ→ひらがな)を通して保持する。
  いずれも名簿にある語(ライム・ミント・まよ・萌・獏・凪)を優先して残す仕組みは維持。
- D17-b【種別枠】アバターの順位表とは別枠で、品名**本体**(バリエーションは見ない)から
  「ワールド」「ワールド用アイテム」「ギミック・ツール」を検出して金額・件数を並べる。
  優先順はワールド>ワールド用>ギミック・ツールで、どれにも当たらなければ種別なし。
  「ギミック付き」「ギミック搭載」は衣装のおまけなので手掛かりから外す。共有カードには
  追加しない。済(1.1.0)。実装メモ: `common.js` の `ITEM_KIND_RULES`・`classifyItemKind`・
  `aggregateByItemKind`、表示は `#/avatars` 内の `avatarKindBox`。
  既知の限界: 「ギミック」は【】の中にあるときだけ種別の名乗りとみなすため、【】を使わない
  純粋なギミック商品は取りこぼす。衣装をギミックに数え違えるより取りこぼす側へ倒している。
- D17-c【ユーザー実環境フィードバック対応(2026-08-09)】済(1.1.0)。3点。
  (1) 種別行のラベルが隣の「〇種類」へ重なっていた。複数対応・未分類用の
  `avatar-other-label`(width:84px)を流用していたのが原因。幅を固定しない
  `avatar-kind-label` を新設した。
  (2) 種別枠で数えた商品がアバター順位表の「未分類」にも二重計上され、未分類が膨らんで
  種別判定が効いていないように見えていた。`aggregateByAvatar` で「手動割り当てが無く・
  アバター照合0件・`classifyItemKind` が空でない」商品を未分類に数えないようにした。
  手動割り当ての一覧には残す(手動割り当ては種別より優先)。
  (3) 素体商品そのもの(「慧 -Kei- オリジナル3Dモデル」等)は同じ素体を2回買わないため
  2商品の昇格則に永久に届かず未分類へ沈んでいた。品名本体に素体商品マーカー
  (`AVATAR_SOLO_MARKER`)がある商品は、マーカー語を除いた残りのトークンを1商品でも昇格させる。
  残りが「和文1語＋英字1語」なら同一アバターの日英併記として英字側をキーにし、日本語側を
  表示名にする(素体商品の題名は自分の名前を名乗る場所で、複数アバターの列挙はまず無いため
  共起候補にせず統合してよい)。残り1語ならそのままバケツ、3語以上なら従来どおり決めない。
  名簿にある名前は従来どおり名簿のキーへ吸わせる。実装は `common.js` の
  `avatarSoloTokens`、索引の `aliases`/`names`、`avatarDisplayName(key, index)`。
- D17-d【ユーザー実環境フィードバック2点(2026-08-09)】済(1.1.0)。
  (1) 「【オリジナル3Dモデル】ラビ先輩」のバケツ名が「先輩」になっていた。原因は2つで、
  ストップ語へ「ラビ」が誤って入っていたことと、素体商品の統合規則が「和1語＋英1語」しか
  扱わず日本語2語の名前を割ってしまうこと。「ラビ」をストップ語から外し、素体商品の残りに
  英字トークンが無いときは飾りを落とした題名まるごと(`avatarSoloName`)を1つのバケツ名に
  するようにした。見つける手掛かりは先頭の語だけに絞る(「先輩」まで手掛かりにすると
  無関係な商品を引き込む)。既知の限界: 「ラビポニー」はカタカナが続いて1語
  (らびぽにー)になるため寄らない。前方一致にすると「凪」が「凪夜」に当たる問題が戻るので、
  寄せないまま手動割り当てへ委ねる。「ラビ先輩用追加素体」のように語として切れるものは寄る。
  (2) 「Halo Ring Hair 95アバター対応」のような体数の名乗りが未分類へ落ちていた。
  複数対応の判定へ `AVATAR_MULTI_COUNT`(数＋アバター/avatars の形)を足し、
  マーカー一覧へ「複数アバター」を追加した。判定は NFKC 正規化後に行うので全角数字も読む。
  判定順は従来どおりなので、バリエーション名で1体に決まる商品(「【25アバター対応】ドレス
  (Milfy)」)は単一のまま。
- D17-e【種別分類の調整(2026-08-09 ユーザー依頼)】済(1.1.0)。5点。
  (1) Udon(UdonSharp・U#)はVRChatのワールド用スクリプト環境なので、これを名乗るギミックを
  ワールド用アイテムへ入れる(`world-item` の pattern へ `\budon|\bu#` を追加)。
  (2) MA(Modular Avatar)は**アバターへ**ギミックを入れる仕組みなので、MA系の名乗りと
  「ギミック」が同じ品名本体に並んでいればアバター用ツールへ入れる(`ITEM_KIND_MA_GIMMICK`)。
  「MA対応」だけでは種別にしない。「ギミック付き」の除外(`ITEM_KIND_IGNORE`)が先に効くため、
  MA対応の衣装は種別なしのまま。`\bma式`のように語頭を縛り、「MAGICAL対応」「Puma対応」で
  誤爆しないことを実測で確認した。
  (3) 種別「髪型」(key `hair`)を新設 → **D17-fで撤回**。
  (4) ハッシュタグ形式「#〇〇3D」も素体商品の名乗りとして `AVATAR_SOLO_MARKER` へ追加し、
  名前の抽出時はハッシュタグを取り除く(`avatarSoloResidue`)。素体でない商品が
  `#Milfy3D` の札を持つ場合は、名前を決められず従来の照合へ落ちるので単一のまま。
  (5) 「【Unity用】Mew【エディタ拡張】」がツールに当たることをテストで固定した。
  表示名のみ「ギミック・ツール」→「アバター用ギミック・ツール」へ変更(保存キーは不変)。
- D17-f【ユーザー実環境フィードバック2点(2026-08-09)】済(1.1.0)。
  (1) 種別「髪型」を撤回。髪型はアバターに着せる買いものなので、複数対応マーカーが
  あれば複数対応、無ければ未分類として扱う(D17-eの(3)を取り消し。優先順は
  world > world-item > tool に戻り、MA式ギミックの格上げは種別なしからのみ)。
  (2) 手動割り当ての一覧を「アバター・複数対応・種別のどれにも当たらなかった商品と
  手動割り当て済みの商品」だけに絞る。種別に当たった商品は集計の未分類からも一覧からも
  外れる(実CSVで276→208商品)。既に保存済みの手動割り当ては種別より優先のまま尊重する。
- D18【沼レポートの再集計ボタン】(2026-08-09 ユーザー要望) 済(1.1.0)。沼レポートの操作部品の
  並びに「再集計」を置く。**BOOTHへは通信しない。**`storage.local` から索引・キャッシュ・
  手動割り当てを読み直し、`render()` で沼レポートを含む全画面を組み直すだけ。
  別タブやポップアップで進んだ収集・変えた割り当てに追従できるようにするのが目的
  (テーマ以外は `storage.onChanged` で追従していないため、開き直す以外に手段が無かった)。
  読み直しの間はボタンを止め、終わったら焦点を戻す(連打で古い読み込みがstateを
  上書きするのを防ぎ、続けて押せるようにする)。新しい保存キーは作らない。
  実装は `dashboard.html` の `avatarRecountBtn` と `dashboard.js` のクリック配線のみ。
- D19【ヘッダーのテーマ切り替えを絵文字だけへ】(2026-08-09 ユーザー要望) 済(1.1.0)。
  文字ラベルを3つ並べると見出し「BOOTHお買いものレポート」が折り返す幅があった。
  ボタンを絵文字だけにし、選択中のモード名(ライト/ダーク/システム)をスイッチの
  真下に小さく出す(`.theme-area`・`#themeModeName`。ボタン側の aria-label と
  aria-pressed が読み上げを担うので、モード名は aria-hidden の飾り)。
  幅420px・640px・1280pxで見出しが1行に収まることを実測した。
- 画面には「アバター名を読み取れた買い物だけの集計で、全支出の内訳ではない」旨を常時出す
  (実測では金額の約55%が未分類=アバター情報を持たない買いもの)。済(1.1.0)

不採用の記録(2026-08-08、再提案防止):
- 品名からのカテゴリ自動分類: BOOTHブラウズページ実測(4カテゴリ×60件)で衣装のrecallが48%
  (ブランド名品名のため)。単独機能の精度に達しない。D14の手動割り当ての補助候補提示に限る。
- アセット管理ツール連携エクスポート: タグ・ジャンル取得に商品ページへの自動アクセスが必要で不適切。

## 委任プロンプト集

依頼時は該当セクションをそのまま受領エージェントへ渡す。各プロンプトは自己完結だが、
リポジトリの `AGENTS.md`(必読)と本書の指摘一覧を参照できることを前提とする。

### 共通前提(全プロンプト冒頭に付ける)

```
対象リポジトリ: D:\GitHub_WorkSpace\booth_purchase_total_extension\booth_purchase_total_extension
必読: AGENTS.md(永続ルール)、docs/improvement-plan.md(指摘の詳細)、HANDOFF.agent.md(現在状態)
ブランチ運用(MUST):
- main と 1.0.0 は直接変更しない。作業開始前に、統合先のバージョンブランチが無ければ
  main(=1.0.0 と同一SHA)から作成する。バグ修正・小変更のみなら 1.0.1、
  UI再設計や機能追加を含むなら 1.1.0 とし、extension/manifest.json の version・README・
  テスト・配布物のバージョン表記を統合先ブランチ名と一致させる。
- 開発用ブランチはバージョンブランチから分岐し、検証後に同じバージョンブランチへ統合する。
- push・公開は行わない。ユーザーの許可が要る。
検証(MUST、完了報告に含める):
- node --check を extension/ と test/ の全 .js に実行(16ファイル全PASS)
- python -m http.server <worktree固有ポート> を起動し /test/index.html が ALL PASS であることと
  check件数を確認(現行640 checks。挙動追加時はテストも追加)
- 構文・ブラウザ実行・実環境確認を分けて報告し、未実行項目をPASSと書かない
- BOOTH実ページのセレクタ・通信は実測していない限り「確認済み」としない
禁止(MUST NOT): extension/icons/*.png の直接編集(変更時は tools/make_icons.py 経由)、
*.csv・booth*.json のコミット、dist/ の上書き、無関係な変更。
```

### P1: UI改善パッケージ(C1〜C8対応)

```
# Objective
BOOTHお買いものレポート拡張(extension/dashboard.html ほか)の画面設計を改善する。
機能・集計ロジックは変更しない。対象は docs/improvement-plan.md の C1〜C11。

# Scope
1. C1: 広い画面ではナビをヘッダー直下の水平タブとして常時表示し、狭い画面のみ現行の
   ハンバーガードロワーにする。ブレークポイントは既存の 620px と整合する値を選び、理由をコメントに残す。
   現行のフォーカス管理・aria属性の水準を維持する。
2. C2: 索引未取得の初回空状態に、3ステップ(ログイン→一括集計→閲覧)の案内を表示する。
3. C3: 月別テーブルの範囲設定操作に視覚的な手掛かりを追加する(既存の .in-range 強調は維持)。
4. C4: 「月ごとの支出」棒グラフにY軸目盛を追加する(累計グラフの描き方と揃える)。
5. C5: メイン合計額の文字サイズを36px以上へ拡大する。
6. C6: 共有パネルの背景テンプレート・画像アップロード・拡大率を「背景をカスタマイズ」
   アコーディオンへ畳む。既定表示はプレビュー+文面+コピー/保存/投稿ボタン。
7. C7: pendingBanner に閉じるボタンを付ける。閉じた状態はページ(タブ)内のみ保持し、
   未収集件数が変化したら再表示する。ストレージへは保存しない。
8. C8: dashboard.html と popup.html に <meta name="viewport" content="width=device-width, initial-scale=1"> を
   追加し、ヒートマップを .table-scroll 相当の横スクロールラッパーで包む。
9. C9: 共有前の確認とキャッシュ削除の確認に使っている confirm() を、既存モーダル
   (author/sharePanel)と同じ作法のアプリ内確認ダイアログへ置き換える。既存の
   「確認してから実行する」分岐と戻り値の意味(承認/取消)を変えない。
10. C10: メイン合計のギフト表記を金額の下の補足行へ移す(フッターの表記と揃える)。
11. C11: ランキングの商品明細開閉の手掛かりを強化する(▸の視認性向上、またはショップ名行全体を
    開閉対象にする。ショップページへのリンクとの競合に注意)。

# Constraints
- 表示の変更であっても「未収集・取得失敗・数字が実際より少ない可能性」の断り書きは一切削らない(MUST NOT)。
- HTML構造を変える場合、test/index.html の対応する要素構成と cases.js のセレクタ前提を同期する。
- CSSは既存の変数(:root)と命名規約に従う。インラインstyleを増やさない。
- 新しい権限・外部リソース読み込みを追加しない(MV3リモートコード禁止)。

# Validation
共通前提の検証に加え、ブラウザで 1280px / 800px / 620px 幅の3段階で全6画面を実表示し、
横スクロールの発生箇所と操作可能性を確認して報告する。

# Completion / Report
変更ファイル一覧、C1〜C8それぞれの対応内容と確認結果、未対応にした項目とその理由を日本語で報告する。
HANDOFF.agent.md の State を現況へ更新する。
```

### P2: 軽微バグ修正(A1〜A3対応)

```
# Objective
docs/improvement-plan.md の A1〜A3 を修正する。パッチ版(1.0.1系)の内容。

# Scope
1. A1: extension/share.js shareConfirmMessage() の文言を実挙動に合わせる。
   収集後に全期間が完全になる場合があることを踏まえ、「取得後、全期間の合計を共有できる場合は
   合計も含めて共有します」という趣旨へ改める。挙動側は変更しない(より正確な数字を出すため)。
2. A2: extension/dashboard.js collectAmounts() の setProgress の比率を (index + 1) / targets.length にし、
   表示テキストと一致させる。
3. A3: dashboard.js の pagehide で boothDashboardTab を消す前に、保存値が自タブのIDと一致するかを
   確認し、一致するときだけ削除する。init() で取得したタブIDを保持して使うこと。

# Constraints
- 各修正に対応するテストを test/cases.js へ追加する(A2は進捗コールバックの検証、
  A1は文言の検証、A3はスタブのstorage操作で検証)。
- 他の指摘(B・C・D)へ手を広げない。

# Validation / Completion
共通前提の検証(node --check 全PASS、/test/index.html ALL PASS+check件数増加)を実施し、
修正内容・テスト件数の差分・検証結果を日本語で報告する。
```

### P3: 効率改善(B1〜B4対応)

```
# Objective
docs/improvement-plan.md の B1〜B4 の無駄な処理を解消する。挙動・表示は変えない。

# Scope
1. B1: render() 冒頭で buildResults() を1回だけ実行し、結果を各描画関数へ引数で渡す。
   renderCurrentView()/renderPendingBanner() など render() 外から呼ばれる経路は、
   直近の結果を再利用できるようにする(状態変更時のみ再計算)。
2. B2: renderOrderTable() の検索入力を150ms程度でdebounceし、shop-asc ソートで使う
   ショップ名ラベルを行ごとに事前計算する。
3. B3: collectAmounts() のキャッシュフラッシュ間隔を、全体書き込みコストが件数に対して
   線形に近づくよう見直す(例: 保存間隔を max(5, floor(総件数/20)) 件ごとにする)。
   中断時にデータを失う範囲が広がりすぎないよう、上限も設ける(例: 50件)。採った値と理由をコメントに残す。
4. B4: oldestCoveredOrder() で暫定最古の sortKey を保持し、再パースを避ける。

# Constraints
- 計測なしの最適化を増やさない。B1・B2はデモデータ(注文1000件相当を合成)での
  before/after の簡易計測(console.time等、計測コードはコミットしない)を報告に含める。
- 「未収集を0として見せない」等の既存の正確性ルールを壊さない。テストがALL PASSのままであること。

# Validation / Completion
共通前提の検証を実施し、変更点・計測結果・検証結果を日本語で報告する。
```

### P4: 機能追加(D1〜D5から選択)

```
# Objective
docs/improvement-plan.md の D1〜D5 のうち、ユーザーが指定した項目を実装する。
指定が無い場合は D2(注文番号リンク)→ D3(タブタイトル進捗)→ D4(ポップアップ今年合計)→
D1(ダークモード)→ D5(ランキング年フィルタ)の順に、小さいものから提案して承認を得る。

# Scope(項目別の要点)
- D1: :root の変数を prefers-color-scheme: dark で上書きする。ハードコードされた色
  (#fff、rgba値、ヒートマップの rgba(252,77,80,α)等)を変数化してから対応する。
  共有カード(canvas描画)は現行配色のまま変えない。
- D2: 注文内訳テーブルの注文番号セルを accounts.booth.pm/orders/{id} へのリンクにする。
  target=_blank rel=noopener。idはBOOTH由来文字列なのでURLへ挿す前に /^\d+$/ 等で検証する。
- D3: publishRunState と同じタイミングで document.title に進捗を反映し、実行終了時に元へ戻す。
- D4: buildSummary() に今年分の合計・件数を追加し、popup.html/popup.js で表示する。
  既存キー boothSummary の後方互換(新フィールドが無い旧データ)を保つ。
- D5: ランキング画面に年セレクト(全期間+注文のある年)を追加し、aggregateByShop の対象を
  絞り込む。共有文面・カードにも選択期間を明記する(全期間でない数字を無断で全期間に見せない)。

# Constraints
- 権限・外部通信・外部リソースを追加しない(MUST NOT)。
- 新挙動には test/cases.js のテストを追加する。共有系(D5)は「数字が実際より少ない可能性」の
  断り書きロジックを期間絞り込み後も維持する。

# Validation / Completion
共通前提の検証を実施し、実装項目・テスト差分・検証結果・未実装項目を日本語で報告する。
```

### P5: Firefox恒久対応(D9、着手前にユーザー確認が必要)

```
# Objective
Firefoxでの恒久利用(署名済み配布)に向けた調査と対応方針の提示。実装・公開はユーザー承認後。

# Scope
1. manifest.json への browser_specific_settings.gecko(id, strict_min_version)追加の影響調査
   (Chrome側で無視されることの確認を含む)。
2. AMO(addons.mozilla.org)署名の手順・所要・審査上の懸念(host_permissions、fetchでの
   credentials include)を整理する。
3. 対応方針・作業見積り・リスクを報告し、実装可否の判断をユーザーへ委ねる。

# Constraints
- このタスクでは調査と文書化のみ(MUST)。manifest変更・アカウント作成・提出を行わない(MUST NOT)。
```

### P9: ギフトフィルタと伏せ字共有(D12・D13対応)

```
# Objective
docs/improvement-plan.md の D12(ギフトフィルタ)と D13(伏せ字共有)を実装する。

# Scope
1. D12: 「集計対象: すべて/自分用/ギフト」のsegmentedフィルタを追加し、buildResults() の入口で
   items[].gift により絞る。絞り込み時の金額は内訳ベース(price×quantity+boost)とし、
   注文単位のお支払金額との差額(送料・クーポン等)を対象別に分けられない旨の行を明示する。
   選択は保存しない(既定「すべて」)。ポップアップは変更しない。
2. D13: 共有パネルに「品名・ショップ名を出さない」チェックを追加。ONのとき共有カードは
   合計額・件数・期間・月別棒グラフのみとし、文面からショップ名を除く。D12と併用可。

# Constraints
- 「未収集・数字が実際より少ない可能性」の断り書きは全モードで維持する(MUST NOT削除)。
- フィルタ中であることをカード・文面に明記し、部分集計を全体に見せない(MUST)。
- 新挙動には test/cases.js のテストを追加し、test/index.html の雛形を同期する(T1テストが検証する)。
- 権限・外部通信を追加しない。share.js の描画変更はD13の表示分岐に限る。

# Validation / Completion
共通前提の検証を実施し、変更点・テスト件数差分・検証結果を日本語で報告する。
```

### P10: 沼レポート(D14対応、大型) 済(1.1.0)

```
# Objective
docs/improvement-plan.md の D14(アバター別支出内訳)を実装する。

# Scope
1. キャッシュ済み items[].name の末尾括弧からバリエーション部を取り出すパーサを common.js 側に置く
   (最後の対応括弧グループを採用。括弧なし・商品名と同文は「バリエーションなし」)。
2. 素体名辞書(日本語+ローマ字の表記集。マヌカ/Manuka、森羅/Shinra、しなの/Shinano、エク/Eku、
   ミルフィ/Milfy 等の主要素体)との照合でアバター別に金額・件数を集計する。品名本体との照合は
   テクスチャ系の補完としてバリエーション照合より低優先で行う。
3. 「複数対応」(Full Pack・複数名一致)と「未分類」を別枠で明示し、未分類には商品単位の
   手動割り当てUIを付ける。割り当てはストレージへ保存し、バックアップ/引っ越しに含める
   (公開契約への追加。README・PRIVACY・AGENTS.md の Project contract を同期する)。
4. ランキング画面または新ビューとしてアバター別ランキングを表示し、共有カード・文面へ
   「最推しアバター」を追加できるようにする(D5の期間フィルタ、D12のギフトフィルタと整合)。

# Constraints
- 商品ページ・検索ページへのアクセス、URL対応マスターの作成を行わない(MUST NOT)。
- 照合失敗を推測で埋めない。未分類は未分類として表示する(MUST)。
- 旧バックアップ(割り当てフィールドなし)の読み込み後方互換を保つ(MUST)。
- 辞書はデータとして分離し、コード変更なしに追記できる形にする。
- 新挙動には test/cases.js のテストを追加する(括弧抽出の境界ケース、辞書照合、手動割り当ての保存)。

# Validation / Completion
共通前提の検証を実施し、辞書の初期収録数・照合できた/できなかった件数の目安・テスト差分・
検証結果を日本語で報告する。
```

## サブエージェント階層ルール(2026-08-07 ユーザー決定)

人間のプロジェクト体制と同様に、役割ごとにコストの安いモデルへ委任する。

- ① マネジメント・計画・レビュー・検品: Fable 5(メインループ)または Opus 5
- ② コーディング・テスト・検証を伴う実装委任(P1〜P4など): Opus 5(`model: "opus"` を明示)
- ③ 自明・機械的タスク(一括置換、定型文書生成、単純調査など): Sonnet 5(`model: "sonnet"` を明示)

補足:
- モデル未指定の委任は親(Fable 5)を継承してしまうため、②③では必ず `model` を明示する。
- エスカレーション: ③で検証不合格や手戻りが続いたら②へ、②で設計判断に迷ったら①へ差し戻す。
- 検証コマンド(node --check・全体テストALL PASS)の必須要件は階層にかかわらず同一。

## 運用メモ

- 優先度の推奨: P2(バグ) → P1(UI) → P3(効率) → P4(機能) → P5(調査)。
  P2のみなら 1.0.1、P1やP4を含む統合なら 1.1.0 を統合先にする。
- 2026-08-08追加分の推奨: P9(D12+D13、確度高・小規模) → P10(D14、大型)。いずれも 1.1.0 へ統合する。
- 本書の指摘を解消した際は、該当項目に「済(バージョン)」を追記し、大きく書き換える場合は
  旧内容を docs/handoff-history.md へ退避する。
