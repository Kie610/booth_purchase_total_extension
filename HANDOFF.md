# 引き継ぎ資料

新しいセッションでこの拡張の開発を続けるための資料です。
コードを読めば分かることは省き、**調査でしか得られなかった知見**と
**作業の進め方**をまとめています。

新しいチャットは次の一文で始められます。

> `D:\GitHub_ClaudeCode\booth_purchase_total_extension` の HANDOFF.md を読んで、
> BOOTHお買いものレポート 拡張機能の開発を引き継いでください。

---

## 前提

- 作業ディレクトリ: `D:\GitHub_ClaudeCode\booth_purchase_total_extension`
- リポジトリ: https://github.com/Kie610/booth_purchase_total_extension (main)
- 日本語で応答すること

## これは何か

BOOTH (accounts.booth.pm/orders) の購入履歴から、実際に支払った合計金額を
**注文単位**で集計するManifest V3のブラウザ拡張です。既存の公開拡張には、
1注文で同じショップの商品を複数買うと購入履歴一覧でその注文が商品数だけ
行として重複表示され、支払金額が多重加算される不具合がありました。
**一覧を注文IDで重複除去してから、注文詳細の「お支払金額」を1注文につき
1回だけ取得する**ことでこれを回避しています。この重複除去がこの拡張の核心です。

## 構成

```
extension/
  manifest.json          MV3。権限は storage と https://accounts.booth.pm/* のみ
  icons/                 16/32/48/128。tools/make_icons.py が原本(手描きしない)
  common.js              定数・日付解析・月/年集計・ギフト表記・ストレージ操作
  purchase-examples.js   X共有用の金額比較マスター(2026-07時点の目安)
  popup.html/css/js      収集済み合計の概要表示と集計ページを開く導線のみ
  dashboard.html/css     集計ページ。実処理はすべてこの専用タブ上で動く
  dashboard-parse.js     BOOTHのHTMLを読む。構造変更で直すのは基本ここ
  dashboard-view.js      画面に出す側。DOM参照と描画。イベントは扱わない
  dashboard.js           状態・イベント配線・BOOTHからの取得(中枢)
tools/make_icons.py      アイコン生成(Pillow使用)
test/                    ブラウザ上で動く簡易テスト
```

集計ページの3ファイルは classic script として同じグローバルを共有します
(モジュールではないので import/export は無く、読み込み順は dashboard.html の
script タグの並びがそのまま依存順です)。境界は「parse=入力 / view=出力 /
dashboard=制御」で、view から state や targetOrders() を読むのは想定内です。

ポップアップは閉じるとJSコンテキストごと破棄されるため、処理は専用タブへ
移してあります。Service Workerでの常駐はChromeでDOMParserが使えず
offscreen APIが必要でFirefox対応が分岐するため、意図的に採用していません。

### ストレージキー

- `boothOrderCache` — `{ [orderId]: { amount, gift, status, date } }`
- `boothOrderIndex` — `{ updatedAt, orders: [{id, status, date}], complete }`
- `boothSummary` — ポップアップ表示用の要約
- `boothRunState` — 実行中の進捗(ポップアップから覗く)
- `boothDashboardTab` — 集計ページのタブID(tabs権限を使わず再フォーカスするため)

## 画面

固定ヘッダー(見出しをアクセント色の帯にし、擬似要素で画面左端まで伸ばす) →
収集済みの合計 → まとめて一括集計 → ①注文履歴の取得 → ②金額の収集 →
年別月別の集計 → 注文ごとの内訳(折りたたみ+スクロール領域) → キャッシュ削除(折りたたみ)。
固定フッターに合計と今年の金額(ギフトは金額の下)・件数・Xでの共有ボタン、
実行中は進捗バーと中断ボタン。フッターの高さは `--footer-height` を
`body.has-progress` で切り替え、本文の下余白も追従させています。

処理は2段階です。①は一覧を**新しい順**に巡回して注文の索引を作り(金額は取らない)、
既知の注文に到達したら停止します。②は年でまとめた表で未収集を見ながら、
月の範囲を指定して詳細ページを取得します。

## 調査済みで、蒸し返さないでほしいこと

### 1. 集計中に出るCSPエラーは仕様であり修正不能

```
Loading the script 'https://www.google.com/recaptcha/enterprise.js?...'
violates ... "script-src 'self'"
```

原因はBOOTHのレスポンスヘッダ `Link: <...enterprise.js>; rel=preload; as=script` で、
ブラウザがHTMLを見るまでもなくヘッダの時点で先読みを試みるためです。取得1件につき
1件記録されます。CSPがブロックしているので通信は発生せず集計にも影響しません。
fetch側から抑止する手段はなく、CSPにgoogle.comを追加するのはMV3のリモートコード禁止に
抵触するため行いません。`dashboard.js` の `fetchDoc` 上のコメントとREADMEに経緯があります。

**HTMLからタグを除去する対応は一度入れて撤去済みです**(ヘッダ起点なので無意味な上、
閉じタグの無いHTMLで必要な部分まで削るリスクだけが残るため)。

### 2. DOMParserで生成した文書からサブリソースは取得されない

script / link(preload, modulepreload, prefetch, stylesheet) / img / iframe の
7種類すべてで通信が発生しないことを、開発環境とユーザーのChromeの両方で
実測確認済みです。無駄な通信の心配は不要で、タグ除去は入れないでください。

### 3. ギフト分の取得方法

ギフトは支払明細の項目**ではなく**、ショップ内の商品グループの見出しです。
実際の注文詳細ページ(保存版)で確認しました。

```html
<div class="sheet-group">
  <b class="u-tpg-title3">ダウンロード商品</b> ... 商品6件 各¥425 ...
</div>
<div class="sheet-group">
  <b class="u-tpg-title3">ギフト</b>          ... 商品6件 各¥425 ...
</div>
```

`.sheet-group` のうち見出しが「ギフト」のものの配下にある `.u-tpg-caption1` の
金額を合計します。BOOSTは `BOOST¥ 0` のようにラベルと金額が同じ要素に入るため、
文字列中の `¥ 金額` を拾う `parseYenAmount` を使います。グループ自体が
見つからない場合は0と断定せず `null`(不明)にします。

実データで お支払金額¥5,100 / ギフト¥2,550、全グループ小計の合計が支払金額と
一致することを検証済みです。

### 4. 設計上の約束

- 月キーは `2026-05` 形式の0埋め。文字列比較がそのまま日付順になり、範囲判定がこれに依存
- 注文日時の表記ゆれ(`2024年5月3日` / `2024/05/03` / `2024-05-03`)を吸収して数値で比較。
  文字列比較では月が1桁のとき順序が崩れる
- 索引の `complete` は、最古まで到達したか、既知の注文に接続でき以前が完全なときのみ true。
  中断や失敗時は finally で取得済み分を保存しつつ未完了として記録する
- フラグを持たない索引(v1.2以前)は完了扱い。当時は全ページ巡回に成功したときだけ保存していたため
- 全件再取得は**最後までやり切ったときだけ**古い索引を置き換える。途中で終わった場合は
  併合する(置き換えると以前取得した範囲まで失われる)
- 金額は5件ごとに逐次保存。中断やタブを閉じても続きから再開できる
- リクエスト間隔は250ms(サーバー負荷軽減)
- 内訳では「未収集」(未取得)と「取得失敗」(取得したが金額を読めなかった)を区別する
- X共有の比較例は `purchase-examples.js` の金額昇順マスターから、合計以下で最も高い項目を選ぶ。
  `target` は探索時の価格帯、`amount` は判定に使う実価格の目安。単品の具体的な商品名だけを使い、
  複数個換算や「高性能な」などの抽象的な修飾は入れない。`category` と `brand` は共有文面には出さず、
  マスターの偏りを検知するために使う。2026-07時点では82項目・18ジャンル・20ブランドで、探索価格帯の
  網羅、12ジャンル以上、15ブランド以上、単一ジャンル25%未満をテストする。価格と更新年月も記録する
- アイコンは `tools/make_icons.py` が原本(#efd4fc の角丸背景に白のB)。PNGは生成物なので
  直接編集しない(差分が追えなくなる)。Bはフォントではなく16x16のドット絵として持ち、
  1ドットを整数倍の正方形で描く(16px→1px、32px→2px、48px→3px、128px→8px)。
  拡大縮小を通さないのでどのサイズでもドットの角が丸まらない。升目を16にしてあるのは
  4サイズすべてが整数倍になる唯一の手頃な値だから。背景の角丸だけは階段状になると汚いので
  スーパーサンプリングして滑らかに描いている

## 進め方

- **テストは必ず実行すること。** node は無いので、Python 3.11 で配信してブラウザで開く。

  ```bash
  python -m http.server 8731
  ```

  リポジトリのルートで起動し `http://localhost:8731/test/index.html` を開く。
  最終行が `ALL PASS` であること。詳細は [test/README.md](test/README.md)。
- ブラウザペインはスクリーンショットが失敗しがちなので、`javascript_tool` で
  DOMの実測値(位置・サイズ・計算後スタイル)を取って確認するのが確実
- 機能を追加したらテストも追加する。拡張APIは `test/stub.js` でスタブ済みで、
  `extension/` の実ファイルをそのまま読み込んで検証している
- コミットメッセージは日本語。何をしたかだけでなく**なぜそうしたか**を書く。
  末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- 機能追加ごとに `manifest.json` の version を上げる
- **push は毎回確認を取ってから。** 勝手に push しない。
  push時は認証情報の都合で `$env:GIT_TERMINAL_PROMPT = "1"` が必要
- BOOTHへ実際にアクセスする部分(ログイン状態でのfetch、ページング、セレクタ)は
  開発環境では検証できない。実行はユーザーに依頼し、結果を確認すること
- **推測で原因を断定しないこと。** 過去にCSPエラーの原因を推測で2回外し、
  実測(securitypolicyviolationイベントの記録、レスポンスヘッダの確認)で解決した

## 残っている候補

- 注文ごとの内訳のソート・絞り込み
- CSV出力
- パッケージ化(アイコン16/32/48/128は用意済み。残るはChromeウェブストア提出。
  ストア掲載用のスクリーンショット1280x800と小タイル440x280が別途必要。
  FirefoxはMV3で host_permissions がユーザー許可制になるため、
  現状のままでは fetch がCORSで弾かれる可能性がある)
